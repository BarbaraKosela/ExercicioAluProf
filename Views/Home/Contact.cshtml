@{
    ViewBag.Title = "Professores";
}
<h2>@ViewBag.Title.</h2>
<h3>@ViewBag.Message</h3>

<p>Nesta página sua consulta de professores fica mais prática, onde o aluno do qual o profissional é responsável, está listado também. Assim como o cadastro de novos!</p>

<form @ViewBag.Store>
    <div>
        <h6>Cadastro de professores:</h6>
    </div>
    <div class="row mt-3">
        <div class="col-md-6" id="div-campo-matricula">
            <label label for="campo-nome">Nome do profissional:</label>
            <input type="text" class="form-control" name="ProfessorModel.NomeProfessor" id="campo-nome" placeholder="Digite seu nome" required="required" />
        </div>
    </div>
    <div>
        <br />
        <button class="btn btn-success" type="submit"><i class="fa fa-save">Salvar</i></button>
    </div>
</form>
<br />
<br />
<form>
    <div class="mr-3 ml-3">
        <h3>Listagem:</h3>
        <table class="table table-striped table table-hover">
            <thead>
                <tr class="bg-primary text-white">
                    <th>Código</th>
                    <th>Nome</th>
                </tr>
            </thead>
            <tbody>
                @foreach (ExercicioAluProf.Models.ProfessorModel aluno in @ViewBag.Professor)
                {
                    <tr>
                        <td>@aluno.IDProfessorResponsavel</td>
                        <td>@aluno.NomeProfessor</td>
                        <td>
                            <a href="/Aluno/Editar?id=@aluno.IDProfessorResponsavel" class="btn btn-outline-success"><i class="fa fa-edit"></i>Editar</a>

                            <a href="/Aluno/Excluir?id=@aluno.IDProfessorResponsavel" class="btn btn-outline-danger">
                                <i class="fa fa-trash"></i>Excluir
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
            <tfoot>
                <tr class="bg-primary text-white">
                    <th>Código</th>
                    <th>Nome</th>
                </tr>
            </tfoot>
        </table>
    </div>
</form>

